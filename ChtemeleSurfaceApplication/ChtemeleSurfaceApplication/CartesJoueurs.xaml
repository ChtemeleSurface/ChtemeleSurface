<s:ScatterViewItem x:Class="ChtemeleSurfaceApplication.CartesJoueurs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="http://schemas.microsoft.com/surface/2008"
             mc:Ignorable="d" 
             Height="150" Width="200">

    <Grid ShowGridLines="True" Background="White" Height="150" Width="200">
        <Grid.Resources>
            <Style TargetType="{x:Type ColumnDefinition}">
                <Setter Property="Width" Value="30"/>
            </Style>
            <Style TargetType="{x:Type Rectangle}">
                <Setter Property="RadiusX" Value="6"/>
                <Setter Property="RadiusY" Value="6"/>
            </Style>
            <Style x:Key="DayOfWeek">
                <Setter Property="Grid.Row" Value="1"></Setter>
                <Setter Property="TextBlock.Margin" Value="5"></Setter>
            </Style>
            <Style x:Key="OneDate">
                <Setter Property="TextBlock.Margin" Value="5"></Setter>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="80" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        
        <!--  <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource DayOfWeek}">NomJoueur</TextBlock> -->


        <TextBlock Name="PseudoCarte" Grid.Column="0" Grid.Row="0" FontSize="12" FontWeight="Bold" >(Nom navigateur)</TextBlock>
        <TextBlock Name="PoseeCarte" Grid.Column="0" Grid.Row="1" FontSize="10" FontWeight="Bold">Dernière carte posée :</TextBlock>
        <TextBlock Name="BonusCarte" Grid.Column="1" Grid.Row="0" FontSize="10" FontWeight="Bold">Bonus : </TextBlock>
        <TextBlock Name="NbPointCarte" Grid.Column="1" Grid.Row="1" FontSize="8" FontWeight="Bold">Nombre de points :</TextBlock>
        

    </Grid>

</s:ScatterViewItem>
